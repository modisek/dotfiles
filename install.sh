#!/usr/bin/env bash  
sudo apt install acpi cpufrequtils xbacklight autocutsel xclip scrot usbmount curl 
   sudo apt install autojump pcmanfm
  sudo apt install git
  sudo apt install software-properties-common 
  sudo apt install alacritty
  sudo apt install rofi
  sudo apt install nodejs npm
  sudo apt install cargo
  sudo apt install msttcorefonts
  sudo apt install mpv ubuntu-restricted-extras
  sudo apt install mopidy
  sudo apt install mpd mpc ncmpcpp
  sudo apt install playerctl mopidy-spotify mopidy-mpd mopidy-local 
  sudo apt install laptop-mode-tools
  sudo apt install anacron
  sudo apt install nitrogen
  sudo apt install xarchiver
  sudo apt install rxvt-unicode
  sudo apt install lolcat figlet 
  sudo apt install polybar
  sudo apt install apktool
  sudo apt install sqlmap
  sudo apt install smartmontools gsmartcontrol neofetch
  sudo apt install wireguard
  sudo apt install httpie jq jo
  sudo apt install openjdk-14-jre-headless
  sudo apt install apt-transport-https ca-certificates curl software-properties-common
  sudo apt install stow
  sudo apt install i3-wm suckless-tools
 sudo apt install erlang
 sudo apt install xsel
  sudo apt-get install j4-dmenu-desktop
  sudo apt-get install openjdk-8-jdk
 sudo  install redshift
  sudo apt install erlang
sudo apt install install xcape
  sudo apt-get install -y exuberant-ctags
  sudo apt install zstd
  sudo apt install wireguard
  sudo apt install openssh-server
  sudo apt install syncthing
  sudo apt install tlp
  sudo apt install xsel
  sudo apt install mosh
  sudo apt install neovim
  sudo apt install zstd
  sudo apt install firefox
  sudo apt install openssh-server
  sudo apt install syncthing
  sudo apt install tlp
